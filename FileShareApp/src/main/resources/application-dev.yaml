spring:
  application:
    name: FileShareApp

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/secure_file_db?ssl=require
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  mail:
    protocol: smtp
    host: live.smtp.mailtrap.io
    port: 587
    ssl:
      enabled: false
    username: api
    password: 284b05b7da2e517d434f023b74fac370
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
#          connectiontimeout: 5000
#          timeout: 5000
#          writetimeout: 5000
#          socketFactory:
#            port: 465
#            class: javax.net.ssl.SSLSocketFactory


server:
  servlet:
    context-path: /api

logging:
  level:
    org:
      springframework:
        security: DEBUG
    root: INFO
    org.springframework.mail: DEBUG
